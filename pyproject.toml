[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "monitor-ai-assistant"
version = "0.1.0"
description = "Monitor AI Assistant application"
readme = "README.md"
requires-python = ">=3.8"

[project.optional-dependencies]
dev = ["pytest", "black", "flake8"]
